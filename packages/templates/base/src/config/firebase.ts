// Firebase config stub — replaced by the orchestrator at generation time.
//
// When this template is used as a generation starting point, the orchestrator
// overwrites this file with the real per-app Firebase config. For standalone
// template development / CI builds, these placeholders keep the module
// compilable but the app will not authenticate against a real project.

import type { FirebaseConfig } from "../components/ui/useAuth";

export const firebaseConfig: FirebaseConfig = {
  apiKey: "REPLACE_AT_GENERATION",
  authDomain: "appio-sandbox.firebaseapp.com",
  projectId: "appio-sandbox",
  appId: "REPLACE_AT_GENERATION",
};
