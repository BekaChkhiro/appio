[project]
name = "appio-codegen"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["appio-shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/appio_codegen"]
