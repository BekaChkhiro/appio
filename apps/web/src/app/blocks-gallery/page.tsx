import { BlocksGallery } from "@/components/blocks-gallery/blocks-gallery";

export const metadata = {
  title: "Blocks Gallery — Appio",
  description:
    "Visual verification surface for @appio/layout-blocks across all 5 theme personas.",
};

export default function BlocksGalleryPage() {
  return <BlocksGallery />;
}
